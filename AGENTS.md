# Agent 工作指引

本文件适用于整个仓库。开始任何分析、编码或文档任务前，必须先阅读 [`description/project.md`](./description/project.md)，建立对当前业务流程、前端编排、实现状态和文档结构的基本理解。

## 开始工作前

1. 阅读 `description/project.md`。
2. 根据其中的“文档导航”，继续阅读与当前任务直接相关的业务文档。
3. 检查当前工作树和相关代码，不要覆盖或回退用户尚未提交的修改。
4. 确认任务涉及的 View、Component、API、State 和文档边界后再开始修改。

不要仅依据根目录 `README.md` 推断业务细节；根 README 用于项目介绍，`description/project.md` 才是 Agent 的项目逻辑入口。

## 代码与注释

- 遵循项目现有 Vue 3 Options API、JavaScript、Vue Router、Axios 和轻量响应式状态的组织方式。
- 优先沿用现有目录职责和数据流，不随意引入新的框架、状态库或大范围抽象。
- 编写代码时，在关键位置添加简洁、准确的中文注释。
- 关键位置包括不直观的业务规则、接口兼容处理、重要异步状态转换、缓存策略、资源释放和容易被错误修改的边界条件。
- 注释应说明“为什么这样处理”或关键约束，不要为普通赋值、简单条件和自解释模板添加逐行注释。
- 后端请求与推荐响应字段使用 snake_case；组件使用的前端模型使用 camelCase。兼容和字段归一化应集中在 `src/api/`，不要扩散到组件层。
- 保留现有移动端布局、加载反馈、二次确认、错误恢复和动画节奏，除非需求明确要求改变。

## 数据库文档

- `description/db/` 默认只读。
- 除非当前任务正在讨论数据库设计，或者用户明确要求修改数据库文档，否则不得编辑、格式化、移动或删除 `description/db/` 中的文件。
- 阅读数据库文档时，只读取与当前接口字段或数据关系直接相关的文件，不需要无差别读取整个目录。
- 新建或修改数据库描述文档时，主体尽量由以下三部分构成：
  1. 表介绍
  2. 字段介绍
  3. 建表语句
- “表介绍”只需说明该表的职责、数据范围和必要的数据关系。
- “字段介绍”应说明字段名称、类型、是否为空、默认值和字段自身语义。
- “建表语句”应提供与字段说明一致、可执行的 DDL。
- 数据库描述文档不要混入过多页面流程、交互状态或跨模块业务编排。需要说明复杂业务逻辑时，应写入对应业务域文档，并在数据库文档中保留简短职责说明或引用。

## 文档同步

- 完成功能后，必须及时更新与该功能直接相关的 `description/` 业务文档。
- 新增或变更接口时，同步记录请求参数、推荐响应、字段映射、错误语义和页面处理方式。
- 修改页面编排或交互状态时，更新对应页面文档；不要把页面交互细节写入数据库文档。
- 当项目业务闭环、目录职责、真实数据范围或已知模拟数据发生变化时，同步更新 `description/project.md`。
- 当对外功能、页面预览、技术栈或启动方式发生变化时，同步更新根目录 `README.md`。
- 文档应描述当前已经实现的行为。未完成方案必须明确标记为计划、建议或待办，不能与当前能力混写。

## 验证与交付

- 代码修改完成后至少运行 `npm run lint`。
- 涉及构建配置、依赖、路由、公共状态或跨模块行为时，同时运行 `npm run build`。
- 只修改文档时，检查相对链接、文件路径和 `git diff --check`。
- 交付时说明修改内容、同步更新的文档以及实际执行的验证；无法执行的验证也要明确说明。
